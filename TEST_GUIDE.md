# Literature Agent 产品测试与验收手册

这份文档用于测试 Literature Agent Windows 本地产品是否可以交给真实用户使用。测试者不需要会编程，绝大多数步骤都在浏览器页面完成。

本文测试的是完整用户流程，而不只是“页面能打开”。正式通过至少需要证明：程序可以安装和启动、设置可以保存、真实文献可以检索、最终 5 篇论文来自至少两个来源、测试邮件与正式日报确实到达，以及 Windows 自动任务可以自行触发日报。

日常使用方法见 [README.md](README.md)。开发者自动化测试不能替代本手册中的人工验收。

## 1. 测试结果怎么填写

每完成一项，在方框中填写结果：

- `[x]`：通过，实际结果与预期一致。
- `[ ]`：尚未测试。
- `失败`：已经测试，但实际结果与预期不一致。
- `阻塞`：因为安装包、网络、账号或上一步失败而无法继续。
- `不适用`：本轮明确不测试该项，并写明原因。

不要只写“有问题”。失败时至少记录测试编号、发生时间、操作步骤、实际结果、截图或错误文字，以及问题能否稳定重现。

## 2. 本轮测试记录

开始前填写下面这张表。API Key、邮箱授权码和密码不要写入测试记录。

| 项目 | 本轮填写 |
| --- | --- |
| 测试日期 | YYYY-MM-DD |
| 测试人员 |  |
| Literature Agent 版本 | 当前候选版 `0.3.0-rc.3` |
| Windows 版本 | 例如 Windows 11 23H2 |
| 测试方式 | Windows 安装包 / 源码开发模式 |
| 浏览器 | Edge / Chrome / 其他 |
| 模型服务商与模型 | 只写服务商和模型名，不写 Key |
| 发件邮箱类型 | 126 / QQ / Gmail / Microsoft 365 / 其他 |
| 测试收件邮箱 | 可以部分隐藏，例如 `e***@126.com` |
| 测试数据目录 | 例如 `D:\LiteratureAgentTestData` |
| 开始时间 |  |
| 结束时间 |  |
| 最终结论 | 通过 / 有条件通过 / 不通过 |

## 3. 发布通过标准

以下项目全部通过，才可以把本版本判定为“可供普通用户下载测试”：

- [ ] 安装包 SHA-256 与 `SHA256SUMS.txt` 一致。
- [ ] 安装、首次启动和再次启动都成功。
- [ ] 程序只通过本机地址 `http://127.0.0.1:8001` 提供页面。
- [ ] 首次设置五个步骤全部完成，其他页面可以正常打开。
- [ ] 模型连接测试通过，来源失败时有清楚的降级说明。
- [ ] 一次真实研究任务正常完成，没有使用 fixture 测试数据。
- [ ] 最终结果达到目标 5 篇，且至少来自两个不同文献来源。
- [ ] 5 篇结果都能看到题目、来源和摘要或明确的摘要缺失提示。
- [ ] SMTP 测试邮件实际到达收件箱。
- [ ] 手动运行订阅后，正式文献摘要邮件实际到达收件箱。
- [ ] 订阅测试邮件和手动正式日报对应的投递记录都是 `sent`。
- [ ] Windows 的 Local 和 Daily 两项自动任务存在。
- [ ] 一个新订阅可以由 Windows 自动任务触发，不需要点击“手动运行”。
- [ ] 关闭浏览器后，本地服务和自动任务仍按产品设计工作。
- [ ] 备份可以创建；在专用测试数据上恢复后，数据与备份一致。
- [ ] 页面不会显示已经保存的真实 API Key 或 SMTP 授权码。
- [ ] 没有 P0 或 P1 问题，所有 P2 问题都有处理结论。

如果只想先确认核心流程，可以先完成第 6 至第 11 节。准备正式发布时，应完成整份手册。

## 4. 测试前准备

### 4.1 准备测试材料

- [ ] 一台 64 位 Windows 10 或 Windows 11 电脑。
- [ ] 当前待测试版本的 Windows 安装包。
- [ ] 与安装包同版本的 `SHA256SUMS.txt`。
- [ ] 一个可用的 OpenAI-compatible 模型 API Key。
- [ ] 模型服务商提供的 API 地址和准确模型名称。
- [ ] 一个已开启 SMTP 的发件邮箱及其授权码或应用密码。
- [ ] 一个可以实际查收邮件的收件邮箱。
- [ ] 稳定的网络连接。
- [ ] 至少 60 分钟测试时间；自动调度测试可能需要额外等待 20 分钟。

Semantic Scholar、OpenAlex 和 PubMed 的 API Key 都是可选项。没有这些 Key 也应能使用公开来源，但 Semantic Scholar 可能更容易遇到 429 限速。

### 4.2 使用专用测试数据目录

建议为本轮创建空目录，例如：

```text
D:\LiteratureAgentTestData
```

不要直接拿正在使用的正式数据做删除、恢复和卸载测试。测试目录中不得放入其他个人文件。

### 4.3 准备统一测试主题

除非本轮专门测试其他学科，建议统一使用下面的数据，便于比较结果：

| 字段 | 测试值 |
| --- | --- |
| 任务名称 | `UAT Agentic RAG YYYYMMDD` |
| 研究主题 | `agentic retrieval augmented generation evaluation` |
| 检索语言 | 中英文 |
| 目标数量 | `5` |
| 起始日期 | 留空 |
| 结束日期 | 留空 |
| 文献来源 | 五个来源全部选择 |
| 证据审查 | 开启 |

`YYYYMMDD` 换成测试当天日期，例如 `20260924`，避免与以前的任务混淆。

### 4.4 测试安全规则

- 不在截图、Issue、聊天或测试记录中展示 API Key、SMTP 授权码和完整密码。
- 测试删除和恢复前，确认当前使用的是专用测试目录。
- 不要在任务运行中强制删除数据库或数据目录。
- 不要连续快速点击“开始执行”或“手动运行”，避免重复消耗模型额度。
- 邮件只发给测试者自己或已经同意接收测试邮件的人。
- 每次失败先记录证据，再刷新页面或重试。

## 5. 安装与启动测试

使用源码开发模式测试时，可以把 T-01 和 T-02 标记为“不适用”，但正式发布安装包前必须在另一台或干净的 Windows 环境完成它们。

### T-01 下载文件完整性

操作：

1. 把安装包和 `SHA256SUMS.txt` 放在同一文件夹。
2. 在该文件夹地址栏输入 `powershell` 并按回车。
3. 执行以下命令，文件名换成本轮版本：

```powershell
Get-FileHash .\Literature-Agent-0.3.0-rc.3-Windows-x64.exe -Algorithm SHA256
```

4. 将输出与 `SHA256SUMS.txt` 对照。

预期结果：两串 SHA-256 完全一致；RC3 应为 `5cfc8b45a68346b6ce5e51282d510d71ae0889b11760f79b05d00aaf2a5b51fc`。

- [ ] T-01 通过

### T-02 全新安装

操作：

1. 双击安装包。
2. 如果 SmartScreen 出现，确认文件来源和 SHA-256 后选择“更多信息 > 仍要运行”。
3. 按安装向导完成安装。
4. 启动 Literature Agent。

预期结果：

- 安装不要求安装 Python 或 Node.js。
- 普通安装不要求管理员权限。
- 默认浏览器自动打开 `http://127.0.0.1:8001`。
- 页面显示 Literature Agent，不出现空白页或连接错误。

- [ ] T-02 通过

### T-03 重复启动保护

操作：保持第一个页面打开，再从开始菜单启动一次 Literature Agent。

预期结果：已有本地服务继续正常运行，不会因端口 8001 冲突启动第二个损坏的实例；浏览器仍能打开产品页面。

- [ ] T-03 通过

### T-04 本地访问边界

操作：查看浏览器地址栏，确认地址以 `127.0.0.1:8001` 开头。

预期结果：产品没有要求登录 Literature Agent 账户，页面不是公网托管页面。

- [ ] T-04 通过

## 6. 首次设置测试

全新数据目录下，首次打开应进入“设置”页。在首次设置完成前，点击“工作台”“每日订阅”或“Prompt 版本”应回到设置页，并提示先完成本机连接设置。

### T-05 数据目录

操作：

1. 点击“选择目录”。
2. 选择第 4.2 节准备的专用测试目录。
3. 等待保存成功提示。

预期结果：页面显示新路径；目录中随后出现数据库、设置、日志或备份文件。

- [ ] T-05 通过

### T-06 模型连接

操作：

1. 填写 API 地址、模型名称和 API Key。
2. 勾选“启用真实检索与模型调用”。
3. 点击“测试连接”。
4. 测试通过后点击“保存”。
5. 刷新页面。

预期结果：连接测试明确显示成功；刷新后状态仍为“已配置”；密码框不显示真实 Key，而是提示已安全保存。

- [ ] T-06 通过

### T-07 模型错误提示

操作：临时把模型名称改成一个明显不存在的名称，点击“测试连接”。记录结果后恢复正确名称并重新测试，不要保存错误配置。

预期结果：页面显示可理解的失败信息，不崩溃、不无限等待，也不把 API Key 显示在错误中。

- [ ] T-07 通过

### T-08 设置页 SMTP 冒烟邮件

操作：

1. 填写 SMTP 主机、端口、账号、授权码、发件人和测试收件人。
2. 465 端口通常选择“直接 SSL”；587 端口通常选择 STARTTLS。
3. 确认 STARTTLS 和直接 SSL 没有同时勾选。
4. 点击“发送测试邮件”。
5. 实际检查收件箱和垃圾邮件箱。
6. 收到后点击“保存”，再刷新页面。

预期结果：收到标题为 `[Literature Agent] SMTP test` 的邮件；页面显示发送成功；刷新后邮件状态仍为“已配置”，且授权码不可见。

- [ ] T-08 页面发送成功
- [ ] T-08 第一封邮件实际到达
- [ ] T-08 刷新后密钥仍隐藏

实际到达时间：`__________`

### T-09 文献来源

操作：可选密钥可以留空；填写 PubMed 联系邮箱后，点击“测试来源”，再点击“保存”。

预期结果：页面分别给出来源测试结果。某个来源临时失败时，应显示具体来源和原因，而不是让整个页面失效。

- [ ] T-09 通过

| 来源 | 成功/失败 | 返回数量或错误摘要 |
| --- | --- | --- |
| Semantic Scholar |  |  |
| OpenAlex |  |  |
| Crossref |  |  |
| arXiv |  |  |
| PubMed |  |  |

### T-10 Jev 默认行为

操作：保持“启用 Jev”关闭，不填写 TypeSafe API Key。

预期结果：页面显示 Jev 已关闭；产品没有阻止模型、来源、订阅或后续检索功能。

- [ ] T-10 通过

### T-11 Windows 自动任务注册

操作：点击“注册自动任务”，等待页面刷新。

预期结果：页面同时显示 `Literature Agent Local` 和 `Literature Agent Daily`。首次设置显示完成，左侧其他页面可以打开。

- [ ] T-11 通过

注意：本步骤只是注册自动任务。真正的自动触发能力要在 T-24 中验证。

## 7. 真实文献检索测试

### T-12 创建任务

操作：打开“工作台”，使用第 4.3 节的统一测试数据，点击“开始执行”。

预期结果：

- 页面立即出现已创建提示和运行状态。
- 一次点击只创建一份运行。
- 状态依次显示检索规划、来源检索、去重规范化、相关性筛选、文献简报、证据审查和保存结果。
- 最终状态变为“已完成”，进度为 `7/7`。

- [ ] T-12 通过

记录 Run ID 前 8 位：`__________`

开始时间：`__________`　完成时间：`__________`　总耗时：`__________`

### T-13 来源诊断

操作：在运行结果中查看“来源诊断”。

预期结果：每个选中来源都有成功、失败或返回数量记录。Semantic Scholar 429 或 arXiv 临时错误应保留清楚的诊断，其他来源仍继续执行。

- [ ] T-13 通过

| 来源 | 状态 | 检索返回数量 | 备注 |
| --- | --- | --- | --- |
| Semantic Scholar |  |  |  |
| OpenAlex |  |  |  |
| Crossref |  |  |  |
| arXiv |  |  |  |
| PubMed |  |  |  |

### T-14 最终 5 篇与多来源要求

操作：逐篇记录最终结果的来源。

预期结果：最终有 5 篇论文，并且来源列中至少出现两个不同来源。这是本轮的硬性发布门槛。

| 序号 | 论文题目简写 | 来源 | DOI 或原文链接可打开 | 摘要可读 |
| --- | --- | --- | --- | --- |
| 1 |  |  | 是 / 否 | 是 / 否 |
| 2 |  |  | 是 / 否 | 是 / 否 |
| 3 |  |  | 是 / 否 | 是 / 否 |
| 4 |  |  | 是 / 否 | 是 / 否 |
| 5 |  |  | 是 / 否 | 是 / 否 |

不同来源数量：`__________`

- [ ] T-14 得到 5 篇
- [ ] T-14 至少两个来源进入最终 5 篇
- [ ] T-14 五篇均无明显重复记录

如果只有一个来源，不能用“总检索时有多个来源成功”代替通过。这里检查的是最终入选的 5 篇。

### T-15 结果质量抽查

操作：任选两篇论文，打开 DOI 或原文页面，对照标题、作者、年份和摘要。

预期结果：页面元数据与原始来源基本一致；中文摘要没有把未提供的信息写成确定事实；推荐理由与研究主题相关。

- [ ] T-15 第一篇抽查通过
- [ ] T-15 第二篇抽查通过

### T-16 阅读标记

操作：分别给论文设置“精读”“保存”“背景”和“忽略”，刷新页面后重新打开该任务。

预期结果：阅读标记被保存，刷新后不丢失，也不会串到其他论文。

- [ ] T-16 通过

## 8. 任务与 Prompt 管理测试

### T-17 历史任务

操作：

1. 在“最近任务”按任务名称搜索。
2. 使用“手动创建”筛选。
3. 打开刚完成的任务。
4. 检查分页按钮；如果只有一页，按钮应正确禁用。

预期结果：搜索、筛选、打开和分页正确；重新打开后仍能看到进度、来源诊断和论文结果。

- [ ] T-17 通过

### T-18 编辑与删除

操作：新建一个临时任务用于测试，编辑名称并保存，再删除它。不要删除 T-12 的验收任务。

预期结果：编辑后名称更新；删除前出现确认提示；确认后临时任务消失，其他任务不受影响。

- [ ] T-18 通过

### T-19 自定义 Prompt 版本

操作：

1. 打开“Prompt 版本”。
2. 选择一个角色，在正文末尾添加容易识别的测试说明。
3. 点击“另存为版本”，再将新版本设为默认。
4. 刷新页面。
5. 把内置版本重新设为默认，再删除测试版本。

预期结果：内置基线未被覆盖；自定义版本可以保存、设为默认和删除；内置版本不能被删除；刷新后默认状态正确。

- [ ] T-19 通过

## 9. 每日订阅与邮件投递记录测试

### T-20 创建订阅

在“每日订阅”填写：

| 字段 | 测试值 |
| --- | --- |
| 订阅名称 | `UAT Agentic RAG 日报 YYYYMMDD` |
| 研究主题 | `agentic retrieval augmented generation evaluation` |
| 收件人 | 本轮实际检查的邮箱 |
| 目标篇数 | `5` |
| 执行时间 | 暂设为未来较晚时间，避免测试中自动触发 |
| 时区 | 实际时区，例如 `Asia/Hong_Kong` |
| 日期范围 | 留空 |
| 来源 | 五个来源全部选择 |

点击“保存订阅”。

预期结果：列表出现名称完整、中文无乱码的新订阅，主题、时间和目标篇数正确。

- [ ] T-20 通过

### T-21 订阅 SMTP 测试

操作：点击订阅右侧的信封图标，并实际查看收件箱和垃圾邮件箱。

预期结果：收到标题为 `[Literature Agent] SMTP test` 的测试邮件；“投递记录”新增 `SMTP 测试`；状态为 `sent`，收件人正确。

- [ ] T-21 邮件实际到达
- [ ] T-21 投递记录为 `sent`

实际到达时间：`__________`

说明：如果 T-08 已经收到设置页测试邮件，T-21 仍应执行，因为它还验证订阅记录与投递历史的关联。

### T-22 手动正式日报

操作：点击该订阅的“手动运行”，等待状态变为“已完成”，然后实际检查收件箱。

预期结果：

- 工作台展示完整进度和来源诊断。
- 最终 5 篇仍至少来自两个来源。
- 收到正式文献摘要邮件，而不是 SMTP 测试模板。
- 邮件包含订阅名称、论文标题、作者、摘要、来源链接或 DOI。
- 投递记录新增“每日摘要”，状态为 `sent`。
- 中文订阅名在页面、邮件主题和正文中均无 `???` 或乱码。

- [ ] T-22 运行状态为“已完成”
- [ ] T-22 最终 5 篇至少来自两个来源
- [ ] T-22 正式日报实际到达
- [ ] T-22 正式投递记录为 `sent`
- [ ] T-22 中文内容无乱码

实际到达时间：`__________`

## 10. Windows 自动运行测试

### T-23 不点击按钮的自动触发

此测试用于证明产品可以真正每天工作。不要把 T-22 的“手动运行”当作自动任务通过。

操作：

1. 新建另一个订阅，名称为 `UAT 自动调度 YYYYMMDD-HHMM`。
2. 把执行时间设为当前时间之后 2 至 5 分钟，时区使用当前真实时区。
3. 确认设置页仍显示 Local 和 Daily 两项任务。
4. 关闭浏览器，但不要退出 Windows、关机或断网。
5. 不点击“手动运行”，等待最多 20 分钟。
6. 重新打开 `http://127.0.0.1:8001`，检查任务、投递记录和邮箱。

预期结果：

- Windows 任务在 15 分钟检查周期内自动发现到期订阅。
- 自动创建的运行最终完成。
- 正式日报自动到达邮箱。
- 投递记录为 `sent`。
- 同一订阅当天没有自动重复发送。

- [ ] T-23 自动创建运行
- [ ] T-23 自动邮件实际到达
- [ ] T-23 投递记录为 `sent`
- [ ] T-23 当天没有重复发送

计划执行时间：`__________`　实际到达时间：`__________`

### T-24 自动任务修复

操作：在“设置 > 自动运行与维护”点击“修复”。

预期结果：Local 和 Daily 两项任务仍存在，操作不会删除订阅、任务或论文结果。

- [ ] T-24 通过

## 11. 备份与恢复测试

只在专用测试数据目录中执行本节。恢复会替换当前数据库中的内容。

### T-25 创建备份

操作：点击“立即备份”，记录返回路径，并在资源管理器中确认文件存在且大小不为 0。

预期结果：`backups` 目录中出现新的 `.db` 文件；创建备份不影响当前页面和任务。

- [ ] T-25 通过

备份路径：`________________________________________`

### T-26 恢复备份

操作：

1. 记录当前任务和订阅数量。
2. 在备份后新建一个名为 `恢复后应消失` 的临时任务，并等待它结束。
3. 确认没有任务处于排队中或运行中。
4. 点击“恢复备份”，选择 T-25 的文件并确认。
5. 刷新页面。

预期结果：备份前的数据仍在；备份后创建的临时任务消失；`backups` 中生成 `before-restore-*.db` 安全副本；已保存密钥状态不受影响。

- [ ] T-26 通过

## 12. 重启、更新与卸载测试

### T-27 重启后持久化

操作：关闭浏览器，重新启动 Literature Agent，再打开工作台、订阅和设置。

预期结果：任务、论文、订阅、阅读标记、普通设置和“已配置”状态仍在；真实密钥仍不可见。

- [ ] T-27 通过

### T-28 检查更新

操作：点击“检查更新”。

预期结果：网络正常时明确提示已是最新版，或显示可打开的 GitHub Release；网络失败时显示可理解的错误，不影响本地功能。

- [ ] T-28 通过

### T-29 覆盖升级

仅在有两个连续版本安装包时执行。先创建备份，再运行较新版本安装包覆盖安装。

预期结果：新版可以启动，原数据目录、任务、订阅和凭据配置状态仍在，数据库迁移没有丢失记录。

- [ ] T-29 通过 / 不适用，原因：`____________________`

### T-30 卸载

只在专用测试电脑或确认可以卸载时执行。从 Windows“已安装的应用”卸载 Literature Agent。

预期结果：程序文件和两项 Windows 计划任务被移除；用户数据目录和 Windows 凭据默认保留。

- [ ] T-30 通过 / 不适用，原因：`____________________`

## 13. 异常与降级测试

### T-31 Semantic Scholar 429

若自然遇到 429，记录来源诊断即可，不要故意高频请求制造限速。

预期结果：系统执行退避或限速，保留错误信息，并继续使用其他来源；最终流程不因单一来源 429 直接崩溃。

- [ ] T-31 通过 / 未遇到 429

### T-32 arXiv 406 或网络失败

若自然遇到 406 或连接失败，记录时间和诊断。

预期结果：兼容请求或网络后备路径生效；即使 arXiv 最终失败，其他来源仍继续，页面能解释失败原因。

- [ ] T-32 通过 / 未遇到该错误

### T-33 断网提示

只在没有任务运行时短暂断网，然后执行“测试来源”或“测试连接”，记录结果后立即恢复网络。

预期结果：页面在合理时间内返回错误，不永久停留在忙碌状态；恢复网络后重新测试可以成功。

- [ ] T-33 通过

### T-34 输入校验

分别尝试以下操作，但不要保存无效数据：

- 任务名称为空时开始执行。
- 所有文献来源都不选择时开始执行。
- 订阅名称、主题、收件人、时间或目标篇数为空时保存。
- 订阅来源全部取消后保存。

预期结果：页面阻止操作并指出缺少内容；不会创建半成品任务或订阅。

- [ ] T-34 通过

## 14. 界面与易用性检查

在常用的 100% 和 125% Windows 缩放比例下查看主要页面。

- [ ] 左侧导航、按钮、输入框和正文没有互相遮挡。
- [ ] 窗口缩小时文字不会超出按钮或卡片。
- [ ] 中文、英文、DOI、长 URL 和长论文标题不会破坏布局。
- [ ] 图标按钮悬停时都有说明。
- [ ] 危险操作有确认提示，普通操作不会反复弹窗。
- [ ] 成功和失败信息容易区分，不遮住主要操作。
- [ ] 键盘 Tab 可以依次到达主要输入框和按钮。
- [ ] 页面刷新后不会意外重复提交任务或订阅。

截图附入报告前，先遮盖邮箱、目录用户名和任何密钥信息。

## 15. 问题严重程度

| 等级 | 含义 | 示例 | 发布处理 |
| --- | --- | --- | --- |
| P0 | 数据或安全灾难 | 删除错误目录、密钥公开、数据库不可恢复损坏 | 立即停止发布 |
| P1 | 核心流程不可用 | 无法启动、无法检索、无法发信、自动任务完全不运行 | 修复并完整回归 |
| P2 | 重要功能错误 | 单个来源持续失败、备份恢复错误、任务记录错乱 | 原则上修复后发布 |
| P3 | 一般体验问题 | 文案不清、局部布局异常、非核心提示不准确 | 可以排期，但必须记录 |

## 16. 缺陷记录模板

每个问题单独复制并填写：

```markdown
### BUG-编号：一句话标题

- 严重程度：P0 / P1 / P2 / P3
- 对应测试：T-XX
- 版本：
- Windows 与浏览器：
- 发生时间：
- 是否稳定重现：是 / 否
- 前置条件：
- 操作步骤：
  1.
  2.
  3.
- 预期结果：
- 实际结果：
- 页面错误文字：
- Run ID 或订阅名称：
- 截图或日志位置：
- 是否已遮盖敏感信息：是 / 否
```

日志默认位于所选数据目录的 `logs` 文件夹。提交日志前应搜索并遮盖邮箱、研究机密和任何疑似密钥。

## 17. 最终验收报告

| 统计项 | 数量 |
| --- | --- |
| 通过 |  |
| 失败 |  |
| 阻塞 |  |
| 不适用 |  |
| P0 |  |
| P1 |  |
| P2 |  |
| P3 |  |

核心证据：

- 最终 5 篇论文来源数量：`__________`
- 设置页 SMTP 冒烟邮件到达：是 / 否
- 订阅测试邮件到达且记录为 `sent`：是 / 否
- 手动正式日报到达：是 / 否
- 自动调度日报到达：是 / 否
- Local 与 Daily 任务存在：是 / 否
- 备份恢复完成：是 / 否

最终决定：

- [ ] 通过：满足全部发布门槛，没有 P0/P1，允许发布。
- [ ] 有条件通过：核心流程通过，仅剩已接受的 P3；写明负责人和计划日期。
- [ ] 不通过：存在 P0/P1，或核心证据缺失，需要修复后重新测试。

结论说明：

```text

```

测试人签字：`__________`　日期：`__________`

## 18. 开发者自动化测试附录

本节只供源码开发者使用。普通产品验收不需要执行命令。

后端测试：

```powershell
cd backend
.\.venv\Scripts\python.exe -m pytest -q
```

前端类型检查与生产构建：

```powershell
cd frontend
npm run build
```

仅执行到期订阅检查：

```powershell
cd backend
.\.venv\Scripts\python.exe -m literature_agent.daily --run-due
```

自动化测试通过只能说明已覆盖的代码行为没有回归。安装体验、真实外部来源、邮件实际到达和 Windows 计划任务仍必须人工验证。
